---
title: تحويل NumPy إلى Excel
type: docs
weight: 30
url: /ar/python-net/convert-numpy-to-excel/
description: تحويل NumPy إلى Excel باستخدام Aspose.Cells for Python via .NET API
keywords: تحويل مصفوفة NumPy إلى Excel باستخدام Python، استيراد مصفوفة NumPy إلى Excel في Python via NET، تحويل مصفوفة NumPy إلى xlsx في Python، تحويل مصفوفة NumPy إلى جدول في Excel بواسطة Python، استيراد مصفوفة NumPy إلى ListObject في Python via NET، تحويل مصفوفة NumPy إلى جدول في Python، تحويل مصفوفة NumPy إلى نطاق، استيراد مصفوفة NumPy إلى نطاق في Python via NET، تحويل مصفوفة NumPy إلى نطاق، تحميل لاستيراد مصفوفة NumPy إلى Excel.
---

## **مقدمة إلى NumPy**
NumPy (Numerical Python) هو امتداد لحسابات Python الرقمية مفتوح المصدر. يمكن استخدام هذه الأداة لتخزين ومعالجة المصفوفات الكبيرة، والتي تكون أكثر كفاءة بكثير من هيكل القائمة المتداخلة في Python (والتي يمكن أيضًا استخدامها لتمثيل المصفوفات). يدعم مجموعة كبيرة من المصفوفات ذات الأبعاد وعمليات المصفوفات، كما يوفر أيضًا عددًا كبيرًا من مكتبات الدوال الرياضية لعمليات المصفوفات. 

الوظائف الرئيسية لـ NumPy:
1. Ndarray ، وهي كائن مصفوفة متعددة الأبعاد ، هي هيكل بيانات سريع ومرن وموفر للمساحة.
1. العمليات الجبر الخطي ، بما في ذلك ضرب المصفوفة ، والانعكاس ، وغيرها.
1. تحويل فورييه ، القيام بتحويل فورييه سريع على مصفوفة.
1. عملية سريعة لمصفوفات الأعداد العائمة.
1. دمج كود اللغة C في Python لجعله يعمل بشكل أسرع.

باستخدام واجهة برمجة التطبيقات Aspose.Cells for Python via .NET ، يمكنك تحويل Ndarray من NumPy إلى Excel و OpenOffice و Pdf و Json والعديد من التنسيقات المختلفة.

## **كيفية تحويل Ndarray من NumPy إلى كتاب العمل في Excel**
إليك مثال لمقتطفات الكود لتوضيح كيفية استيراد البيانات من مصفوفة NumPy إلى ملف Excel باستخدام Aspose.Cells for Python via .NET:
١. إنشاء بيانات مصفوفة NumPy عينة.
١. تجاوز مصفوفة NumPy واستيراد البيانات باستخدام Aspose.Cells لـ Python via .NET.

{{< gist "aspose-cells-gists" "7bb30376b4d40cdfd596286870fb9752" "Numpy-import-ndarray-to-excel.py" >}}

نتيجة الإخراج:
<br>
<img src="ndarray_excel_workook.png" width=60% />

## **كيفية تحويل Ndarray من NumPy إلى ورق العمل**
إليك مثال لمقتطفات الكود لتوضيح كيفية استيراد البيانات من مصفوفة NumPy إلى ملف Excel باستخدام Aspose.Cells for Python via .NET:
١. إنشاء بيانات مصفوفة NumPy عينة.
١. تجاوز مصفوفة NumPy واستيراد البيانات باستخدام Aspose.Cells لـ Python via .NET.

{{< gist "aspose-cells-gists" "7bb30376b4d40cdfd596286870fb9752" "Numpy-import-ndarray-to-worksheet.py" >}}

نتيجة الإخراج:
<br>
<img src="ndarray_excel.png" width=60% />

## **كيفية تحويل Ndarray من NumPy إلى ListObject**
إليك مثال لمقتطفات الكود لتوضيح كيفية استيراد البيانات من مصفوفة NumPy إلى ListObject باستخدام Aspose.Cells for Python via .NET:
١. إنشاء بيانات مصفوفة NumPy عينة.
١. تجاوز مصفوفة NumPy واستيراد البيانات باستخدام Aspose.Cells لـ Python via .NET.
١. قم بإنشاء كائن ListObject باستخدام بيانات عينة.

{{< gist "aspose-cells-gists" "7bb30376b4d40cdfd596286870fb9752" "Numpy-import-ndarray-to-listobject.py" >}}

نتيجة الإخراج:
<br>
<img src="ndarray_table.png" width=60% />

## **كيفية تحويل ndarray من NumPy إلى Range**
إليك مقتطف شفرة مثال لتوضيح كيفية استيراد البيانات من مصفوفة NumPy إلى نطاق باستخدام Aspose.Cells لـ Python via .NET:
١. إنشاء بيانات مصفوفة NumPy عينة.
١. تجاوز مصفوفة NumPy واستيراد البيانات باستخدام Aspose.Cells لـ Python via .NET.
١. إنشاء كائن Range باستخدام بيانات عينة.

{{< gist "aspose-cells-gists" "7bb30376b4d40cdfd596286870fb9752" "Numpy-import-ndarray-to-range.py" >}}

نتيجة الإخراج:
```
row count: 4
column count: 3
```

## **كيفية تحويل ndarray من NumPy إلى Name**
إليك مقتطف شفرة مثال لتوضيح كيفية استيراد البيانات من مصفوفة NumPy إلى اسم باستخدام Aspose.Cells لـ Python via .NET:
١. إنشاء بيانات مصفوفة NumPy عينة.
١. تجاوز مصفوفة NumPy واستيراد البيانات باستخدام Aspose.Cells لـ Python via .NET.
١. إنشاء كائن Name باستخدام بيانات عينة.

{{< gist "aspose-cells-gists" "7bb30376b4d40cdfd596286870fb9752" "Numpy-import-ndarray-to-name.py" >}}

نتيجة الإخراج:
<br>
<img src="ndarray_name.png" width=60% />
